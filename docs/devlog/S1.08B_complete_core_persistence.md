# Development Log — S1.08B

## 1. Stage Identity

- Stage ID: S1.08B
- Stage name: Complete Core Persistence
- Agent status: COMPLETE
- Date/time: 2026-08-14 22:54:33 CST (Asia/Shanghai)
- Branch: `dev/oae-v0.1`

## 2. Scope

- Assigned objective: Complete the OAE v0.1 core persistence layer by adding
  the four tables frozen in the S1.08A Persistence Contract Errata.
- Explicit in-scope work: Append `core.index_sessions`, `core.macro_events`,
  `core.fee_schedules`, and `core.decision_option_snapshots` to the existing
  core migration; extend the existing DuckDB metadata and behavior tests;
  preserve every approved S1.08 table contract and S1.03B Errata B1–B4; and
  verify the exact ten-table application contract.
- Explicit out-of-scope work: Research or audit schemas/tables and SQL;
  application source; DTOs; ingestion; migration runner; lineage/snapshot
  serialization; foreign keys; defaults; standalone indexes; dependencies;
  frozen configuration; canonical specifications; CI; README; and S1.09.
- Acceptance criteria: All four frozen core tables have exact ordered columns,
  types, nullability, and primary keys; the macro-event CHECK accepts exactly
  the two frozen values; nullable fee schedules and invalid decision snapshots
  remain storable; exactly ten application tables exist; prior contracts remain
  unchanged; focused/full tests and exact-code CI pass; and only authorized
  paths change in each commit.

## 3. Specification Baseline

- Frozen Math Spec SHA256:
  `a612c66a23206e43b149c207f88a3de26e8c8e037cbf4eddd5ac9a3289e15dbe`
- Data/Backtest Spec SHA256:
  `81e0cd4f1b67cf29bb9eb1b2c6669ede750483027328cfb72d4c4c8b3252dd3f`
- Implementation Spec SHA256:
  `68a9a1e576d7f0d649e1c74885bd7327df565df7e9fc1c91667110b15aef7970`
- Implementation Errata SHA256:
  `67dda37487d03902af90fd03a6afd8e24c4f962e64cdcadfda971ec10cd4fc6f`
- Persistence Errata SHA256:
  `58a47519135187c65b99167546e15b797ac50a89342279a9e2c7eb4f3c17bc30`
- CHANGE_CONTROL SHA256:
  `114b684c07ae6880ca9089c64346677b85b1ab88f94b4c7e80c0ade285fff221`
- SPEC_MANIFEST SHA256:
  `6a5f15cf31b78c23617ba8a16281d07076e8f6d584c412fafc5b94ff89ad0e20`
- Raw config SHA256:
  `a8043d2e8959fa8073951406bdef103afb1a45163861db09cdd66a261f3f6fa0`
- Normalized config SHA256:
  `ece897f1dd038743462322480aec20aba05b219ce7af8b2e4e38f7a013f11044`

All six manifest entries, the manifest identity, the raw YAML identity, and
the normalized frozen configuration identity matched the assigned baseline
before implementation and at the implementation-commit checkpoint.

## 4. Git Baseline

- Base commit SHA: `9d32e1f17f1134cd3e63d1b3d0240c59d87a3c9b`
- Branch: `dev/oae-v0.1`
- Initial working-tree status: Clean
- Expected remote: `https://github.com/XXHANOO/OAE.git`, branch
  `refs/heads/dev/oae-v0.1`
- Implementation commit SHA:
  `74b32633e8ebbc6c5e1a674e7b4f843fa62f8b0d`

## 5. Files Changed

### Added

- `docs/devlog/S1.08B_complete_core_persistence.md`: Records the immutable
  S1.08B scope, implementation, verification, CI, and compliance evidence.

### Modified

- `sql/001_core.sql`: Appends exactly the four S1.08A-authorized core table
  definitions without changing the original S1.08 DDL.
- `tests/unit/test_core_schema.py`: Expands the exact table inventory and adds
  metadata and behavior coverage for the four new tables while preserving the
  semantic meaning of all existing S1.08 tests.
- `docs/devlog/INDEX.md`: Marks S1.08A APPROVED and registers S1.08B as
  COMPLETE with review PENDING.

### Deleted

- NONE

`sql/.gitkeep` remains present and unchanged. The implementation commit
contains only the SQL and test modifications; the report and index belong
only to the separate report commit.

## 6. Implementation Details

The four ordinary `CREATE TABLE` statements were appended after the approved
S1.08 DDL. No existing column, order, type, nullability, key, or CHECK was
changed. No table uses `IF NOT EXISTS`.

`core.index_sessions` implements the nine frozen columns and primary key
`(session_date_et)`. `core.macro_events` implements its eight frozen columns,
primary key `(event_id)`, and the explicitly required CHECK allowing only
`FOMC_DECISION` and `FOMC_PRESS_CONFERENCE`. `core.fee_schedules` implements
its nine frozen columns with nullable `effective_to` and no primary key.
`core.decision_option_snapshots` implements its eleven frozen columns and
ordered primary key `(session_date_et, contract_id)`.

The existing test fixture still uses pytest `tmp_path`, executes the actual
`sql/001_core.sql`, inspects DuckDB metadata, and closes every connection.
Existing metadata helpers remain in use. New helper inserts exercise the
macro-event domain and decision-snapshot evidence boundary.

### Complete Core Persistence Contract

Schemas created:

```text
meta
core
```

Total application tables: 10

Meta tables: 1

```text
meta.raw_file_manifest
```

Core tables: 9

```text
core.trading_sessions
core.index_bars_1m
core.index_sessions
core.option_contracts
core.option_nbbo
core.option_settlements
core.macro_events
core.fee_schedules
core.decision_option_snapshots
```

- S1.08 original tables changed semantically: NO
- S1.03B B1–B4 preserved: YES
- S1.08A additional core tables implemented: 4 / 4
- Research schema created: NO
- Audit schema created: NO
- Research SQL created: NO
- Audit SQL created: NO
- Migration runner created: NO
- Application source modified: NO
- Dependencies modified: NO

### Four New Tables

- `core.index_sessions`: IMPLEMENTED
- `core.macro_events`: IMPLEMENTED
- `core.fee_schedules`: IMPLEMENTED
- `core.decision_option_snapshots`: IMPLEMENTED
- `macro_events` CHECK enforced: YES
- Allowed event types: `FOMC_DECISION`, `FOMC_PRESS_CONFERENCE`
- `fee_schedules` primary key: NONE
- `fee_schedules.effective_to`: NULLABLE
- `decision_option_snapshots` primary key:
  `(session_date_et, contract_id)`
- Invalid snapshot persistence test: PASS

### Evidence Boundary

Raw `core.option_nbbo` still permits provider-originated invalid, crossed, and
zero-size observations. Derived `core.decision_option_snapshots` still permits
`quote_valid = false` observations, including crossed bid/ask, zero sizes, and
`quote_age_ms > 2000`.

No quote freshness, crossed-market, zero-size, or bid/ask validity business
rule was incorrectly enforced by DDL.

No foreign key, standalone UNIQUE constraint, DEFAULT clause, standalone
index, or unapproved CHECK was added.

## 7. Tests and Verification

Focused core schema tests:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python -m pytest tests/unit/test_core_schema.py
```

Result: 20 passed, 0 failed, 0 skipped in 0.37 seconds.

Full local regression:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python -m pytest
```

Result: 118 passed, 0 failed, 0 skipped in 0.53 seconds.

Normalized config regression:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python - <<'PY'
from oae.config import compute_config_sha256, load_config, validate_frozen_v0_1

config = load_config("config/oae_v0_1_frozen.yaml")
digest = compute_config_sha256(config)

assert digest == (
    "ece897f1dd038743462322480aec20aba05b219ce7af8b2e4e38f7a013f11044"
)

assert validate_frozen_v0_1(config) is None

print(digest)
PY
```

Result: Exact normalized digest printed; validation returned `None`.

Canonical integrity:

```text
shasum -a 256 -c docs/specs/SPEC_MANIFEST.sha256
shasum -a 256 docs/specs/SPEC_MANIFEST.sha256
shasum -a 256 docs/specs/04_OAE_v0.1_PERSISTENCE_ERRATA.md
shasum -a 256 config/oae_v0_1_frozen.yaml
```

Result: All six canonical entries returned `OK`; manifest, Persistence Errata,
and raw config hashes matched the assigned baseline.

Pre-commit scope verification:

```text
git diff --check
git diff --name-status
git diff --stat
git status --short
git diff --cached --check
git diff --cached --name-status
git diff --cached --stat
```

Result: Whitespace checks passed. Before the implementation commit, the only
changed and staged paths were exactly:

```text
M  sql/001_core.sql
M  tests/unit/test_core_schema.py
```

No `sql/002_research.sql` or `sql/003_audit.sql` existed.

Implementation commit:

```text
74b32633e8ebbc6c5e1a674e7b4f843fa62f8b0d
```

Implementation GitHub Actions CI:

- Workflow: `CI`
- Run ID: `31811717210`
- Run URL: `https://github.com/XXHANOO/OAE/actions/runs/31811717210`
- Exact head SHA: `74b32633e8ebbc6c5e1a674e7b4f843fa62f8b0d`
- Event: `push`
- Status: completed
- Conclusion: SUCCESS
- Job: `test`; job ID `94803670535`
- Checkout: PASS
- Verify frozen specification manifest: PASS, all six entries
- Set up Python 3.12: PASS, CPython 3.12.13
- Install project: PASS
- Verify package import: PASS
- Run tests: PASS, 118 passed, 0 failed, 0 skipped in 1.18 seconds

## 8. Specification Compliance

- Mathematical rules changed: NO
- Feature definitions changed: NO
- Market-data semantics changed: NO
- Timestamp semantics changed: NO
- Execution assumptions changed: NO
- Risk/Edge thresholds changed: NO
- Dependencies changed: NO
- Assigned scope changed or exceeded: NO
- Look-ahead exposure introduced: NO
- Existing S1.08 persistence semantics changed: NO
- S1.03B B1–B4 weakened or altered: NO
- Frozen YAML, canonical specs, manifest, CI, README, or source changed: NO
- Research or audit persistence created: NO
- Migration runner, DTO, ingestion, or lineage serialization created: NO

## 9. Deviations

NONE

## 10. Known Limitations / Risks

- Warm-up 504–755 operational semantics: UNRESOLVED — future model-readiness
  implementation.
- Branch protection: NOT ADDRESSED.
- GitHub Action immutable SHA pinning: NOT ADDRESSED.
- Build backend full locking: NOT ADDRESSED.
- S1.08A `macro_events` optional-CHECK wording: RESOLVED FOR S1.08B — the
  CHECK was explicitly required by Human + ChatGPT authorization and is
  implemented.
- S1.08A `daily_decisions.max_loss` physical-type wording: UNRESOLVED P3 —
  belongs to S1.09, not S1.08B.
- Dataset snapshot serialization/hashing: DEFERRED to a future dedicated
  lineage/snapshot stage before formal backtest.
- `forecast_error`: DEFERRED — no frozen semantic definition.
- No research SQL yet.
- No audit SQL yet.
- No migration runner.
- No DTOs.
- No ingestion.
- No foreign-key lineage enforcement.

These are carried-forward risks or intentional current boundaries.

## 11. Verification State

- Working tree clean at implementation-commit checkpoint: YES
- Local HEAD at report creation:
  `74b32633e8ebbc6c5e1a674e7b4f843fa62f8b0d`
- Remote HEAD at report creation:
  `74b32633e8ebbc6c5e1a674e7b4f843fa62f8b0d`
- Implementation commit pushed: YES
- Exact implementation-commit CI completed successfully: YES
- Report commit pushed: PENDING AT REPORT CREATION; verify after commit without
  rewriting this historical report.
- Final report-commit CI: PENDING AT REPORT CREATION; verify after push without
  rewriting this historical report.

## 12. Next Proposed Stage

S1.09 — Reconciled Research SQL Migration, only after Human + ChatGPT approve
S1.08B.

STOPPING FOR HUMAN / CHATGPT AUDIT.
