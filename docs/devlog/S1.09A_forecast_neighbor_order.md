# Development Log — S1.09A

## 1. Stage Identity

- Stage ID: S1.09A
- Stage name: Forecast Neighbor Column Order Correction
- Agent status: COMPLETE
- Date/time: 2026-08-14 23:46:08 CST (Asia/Shanghai)
- Branch: `dev/oae-v0.1`

## 2. Scope

- Assigned objective: Correct the physical column order of
  `research.forecast_neighbors` to the already-frozen canonical Persistence
  Errata and correct only its matching exact-order regression expectation.
- Explicit in-scope work: Reorder the five final columns in the existing
  `research.forecast_neighbors` DDL; preserve all ten columns, types,
  nullability, table identity, and composite primary key; keep the test as an
  exact ordered comparison of real DuckDB `information_schema` metadata; and
  create this corrective audit trail.
- Explicit out-of-scope work: Every other research table; core or audit SQL;
  canonical specifications; configuration; application source; dependencies;
  CI configuration; mathematical, point-in-time, market-data, model,
  execution, fee, risk, Edge, and trading semantics; refactors; formatting
  sweeps; and S1.10.
- Acceptance criteria: The canonical ten-column physical order is reported by
  DuckDB on a fresh database after applying `001_core.sql` and
  `002_research.sql`; the exact-order regression matches that order; focused
  and full tests pass; canonical/config identities remain unchanged; each
  commit contains only its authorized paths; both exact-SHA CI runs succeed;
  and the index records S1.09 as REJECTED and S1.09A as PENDING review.

## 3. Specification Baseline

- Frozen Math Spec SHA256:
  `a612c66a23206e43b149c207f88a3de26e8c8e037cbf4eddd5ac9a3289e15dbe`
- Data/Backtest Spec SHA256:
  `81e0cd4f1b67cf29bb9eb1b2c6669ede750483027328cfb72d4c4c8b3252dd3f`
- Implementation Spec SHA256:
  `68a9a1e576d7f0d649e1c74885bd7327df565df7e9fc1c91667110b15aef7970`
- Implementation Errata SHA256:
  `67dda37487d03902af90fd03a6afd8e24c4f962e64cdcadfda971ec10cd4fc6f`
- Persistence Errata SHA256:
  `58a47519135187c65b99167546e15b797ac50a89342279a9e2c7eb4f3c17bc30`
- CHANGE_CONTROL SHA256:
  `114b684c07ae6880ca9089c64346677b85b1ab88f94b4c7e80c0ade285fff221`
- SPEC_MANIFEST SHA256:
  `6a5f15cf31b78c23617ba8a16281d07076e8f6d584c412fafc5b94ff89ad0e20`
- Raw config SHA256:
  `a8043d2e8959fa8073951406bdef103afb1a45163861db09cdd66a261f3f6fa0`
- Normalized config SHA256:
  `ece897f1dd038743462322480aec20aba05b219ce7af8b2e4e38f7a013f11044`
- `sql/001_core.sql` SHA256:
  `c339c716b00b2b67be11c2fc0d0b9d09396d7f86bae587666ae9f155f7b3b758`
- Immutable S1.09 report SHA256:
  `07956d9233ffd80068223188cf54a583a8ad9d19bd0400afc7006fbad3accacc`

All six canonical files and the manifest identity matched before
implementation and before the implementation commit. The normalized frozen
configuration and protected core/report identities also matched.

## 4. Git Baseline

- Base commit SHA: `b22ab3d0d11bb604a75a6cc386f1cdc57d0e0b88`
- Branch: `dev/oae-v0.1`
- Initial working-tree status: Clean
- Expected remote: `https://github.com/XXHANOO/OAE.git`, branch
  `refs/heads/dev/oae-v0.1`
- Implementation commit SHA:
  `9233ecaeb14b2685650658bf91dc7376d86ddc26`

## 5. Files Changed

### Added

- `docs/devlog/S1.09A_forecast_neighbor_order.md`: Records the defect, root
  cause, correction, tests, CI, scope, and historical audit state.

### Modified

- `sql/002_research.sql`: Reorders only the final five physical columns of
  `research.forecast_neighbors` to the canonical Persistence Errata order.
- `tests/unit/test_research_schema.py`: Corrects only the corresponding exact
  ordered-column expectation used against DuckDB metadata.
- `docs/devlog/INDEX.md`: Marks S1.09 REJECTED and registers S1.09A as
  COMPLETE with review PENDING.

### Deleted

- NONE

The implementation commit contains only the SQL and research-test paths. The
report commit contains only this report and the index. The original S1.09
report remains untouched.

## 6. Implementation Details

The `research.forecast_neighbors` DDL now declares its columns in this exact
physical order:

```text
forecast_id
neighbor_rank
neighbor_session_date
distance
terminal_return
similarity_weight_raw
recency_weight
combined_weight_raw
normalized_weight
age_sessions
```

The existing composite primary key remains:

```text
(forecast_id, neighbor_rank)
```

The existing `test_forecast_neighbors_full_weight_contract` expectation now
uses the same order. The helper continues querying
`information_schema.columns`, ordering by `ordinal_position`, and comparing
the complete ordered `(column_name, data_type, is_nullable)` list. The test
therefore validates actual schema metadata from a fresh temporary DuckDB
database rather than SQL text or an unordered set.

## S1.09 Audit Defect and Root Cause

- S1.09 Human + ChatGPT review: REJECTED / FIX REQUIRED
- Severity: P2
- Affected table: `research.forecast_neighbors`
- Defect: The physical column order differed from the canonical Persistence
  Errata, while names, types, nullability, and primary-key semantics were
  otherwise correct.

Canonical order:

```text
forecast_id
neighbor_rank
neighbor_session_date
distance
terminal_return
similarity_weight_raw
recency_weight
combined_weight_raw
normalized_weight
age_sessions
```

S1.09 implemented order:

```text
forecast_id
neighbor_rank
neighbor_session_date
distance
terminal_return
age_sessions
normalized_weight
similarity_weight_raw
recency_weight
combined_weight_raw
```

Root cause: The Human + ChatGPT S1.09 task prompt supplied the incorrect
order. Codex followed that task prompt correctly, but canonical repository
specifications have higher authority. This is not characterized as Codex
independently violating the specification.

The existing immutable `docs/devlog/S1.09_reconciled_research_sql.md` remains
an historical record of what the agent believed at completion time. Its
`Deviations: NONE` statement was later disproven by independent Human +
ChatGPT audit. S1.09A provides the corrective audit trail rather than
rewriting history.

## S1.09A Correction

- Column names changed: NO
- Types changed: NO
- Nullability changed: NO
- Primary key changed: NO
- Mathematical semantics changed: NO
- Point-in-time semantics changed: NO
- Model weights changed: NO
- Only physical column order changed: YES
- Corresponding exact-order regression expectation corrected: YES
- Other eleven research tables changed: NO
- Core SQL changed: NO
- Audit SQL created: NO
- Canonical files changed: NO

The baseline `sql/002_research.sql` SHA256 was
`b0047b3e62f187ccab33db38a3ec6cd8909ec3f11e1ce16c7fcf0802511b58bd`.
The corrected migration SHA256 is
`9bc128dfb1df905bda5132c4f70b4d4b1eea411452538001d94a8a5358439e4d`.
The targeted diff contains only the ordered movement inside
`research.forecast_neighbors`; every other research table block is unchanged.

## 7. Tests and Verification

Focused research-schema tests:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python -m pytest tests/unit/test_research_schema.py
```

Result: 26 passed, 0 failed, 0 skipped in 0.52 seconds.

Full local regression:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python -m pytest
```

Result: 144 passed, 0 failed, 0 skipped in 1.02 seconds.

Normalized-config regression:

```text
source /private/tmp/oae-s107.q3CBSD/venv/bin/activate
python - <<'PY'
from oae.config import compute_config_sha256, load_config, validate_frozen_v0_1

config = load_config("config/oae_v0_1_frozen.yaml")
digest = compute_config_sha256(config)

assert digest == (
    "ece897f1dd038743462322480aec20aba05b219ce7af8b2e4e38f7a013f11044"
)

assert validate_frozen_v0_1(config) is None

print(digest)
PY
```

Result: Exact normalized digest printed; validation returned `None`.

Canonical integrity:

```text
shasum -a 256 -c docs/specs/SPEC_MANIFEST.sha256
shasum -a 256 docs/specs/SPEC_MANIFEST.sha256
shasum -a 256 docs/specs/04_OAE_v0.1_PERSISTENCE_ERRATA.md
```

Result: All six canonical entries returned `OK`; the manifest and Persistence
Errata identities matched the assigned values.

Targeted and pre-commit diff audit:

```text
git diff -- sql/002_research.sql
git diff -- tests/unit/test_research_schema.py
git diff --check
git diff --name-status
git diff --stat
git status --short
git diff --cached --check
git diff --cached --name-status
git diff --cached --stat
```

Result: Whitespace checks passed. The only changed and staged paths were:

```text
M  sql/002_research.sql
M  tests/unit/test_research_schema.py
```

The SQL diff showed only the five-field order correction inside
`research.forecast_neighbors`. The test diff showed only the same correction
to the exact ordered-column contract.

Implementation commit:

```text
9233ecaeb14b2685650658bf91dc7376d86ddc26
```

Implementation GitHub Actions CI:

- Workflow: `CI`
- Run ID: `31816016794`
- Run URL: `https://github.com/XXHANOO/OAE/actions/runs/31816016794`
- Exact head SHA: `9233ecaeb14b2685650658bf91dc7376d86ddc26`
- Event: `push`
- Status: completed
- Conclusion: SUCCESS
- Job: `test`; job ID `94817762785`
- Checkout: PASS
- Verify frozen specification manifest: PASS, all six entries
- Set up Python 3.12: PASS
- Install project: PASS
- Verify package import: PASS
- Run tests: PASS, 144 passed, 0 failed, 0 skipped in 2.38 seconds

## 8. Specification Compliance

- Mathematical rules changed: NO
- Feature definitions changed: NO
- Market-data semantics changed: NO
- Timestamp semantics changed: NO
- Execution assumptions changed: NO
- Risk/Edge thresholds changed: NO
- Dependencies changed: NO
- Assigned scope changed or exceeded: NO
- Look-ahead exposure introduced: NO
- Column names, types, nullability, or primary key changed: NO
- Other research tables changed: NO
- Existing meta/core persistence changed: NO
- Frozen YAML, canonical specs, manifest, CI, README, or source changed: NO
- Audit schema/table or SQL created: NO
- S1.10 started: NO
- Frozen persistence contract restored: YES

## 9. Deviations

NONE

## 10. Known Limitations / Risks

- S1.09 remains REJECTED as an historical review result; S1.09A requires
  independent Human + ChatGPT audit.
- Warm-up 504–755 operational semantics remain deferred to future model work.
- Dataset snapshot deterministic serialization/hash remains deferred to a
  dedicated lineage/snapshot stage before formal backtesting.
- `forecast_error` remains deferred because no frozen semantic definition
  exists.
- No audit SQL or migration runner exists yet.
- No research DTO, ingestion, model, replay, or backtest implementation exists
  yet.
- No foreign-key lineage enforcement exists by frozen design.
- Branch protection, GitHub Actions immutable SHA pinning, and full build
  backend locking remain unaddressed carried-forward governance risks.

## 11. Verification State

- Working tree clean at implementation-commit checkpoint: YES
- Local HEAD at report creation:
  `9233ecaeb14b2685650658bf91dc7376d86ddc26`
- Remote HEAD at report creation:
  `9233ecaeb14b2685650658bf91dc7376d86ddc26`
- Implementation commit pushed: YES
- Exact implementation-commit CI completed successfully: YES
- Report commit pushed: PENDING AT REPORT CREATION; verify after commit without
  rewriting this historical report.
- Final report-commit CI: PENDING AT REPORT CREATION; verify after push without
  rewriting this historical report.
- `sql/001_core.sql` unchanged: YES
- `sql/002_research.sql` present and corrected: YES
- `sql/003_audit.sql` absent: YES
- Original S1.09 report unchanged: YES
- S1.10 started: NO

## 12. Next Proposed Stage

S1.10 — Reconciled Audit SQL Migration, only after Human + ChatGPT approve
S1.09A.

STOPPING FOR HUMAN / CHATGPT AUDIT.
