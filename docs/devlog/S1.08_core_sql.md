# Development Log — S1.08

## 1. Stage Identity

- Stage ID: S1.08
- Stage name: Core SQL Migration
- Agent status: COMPLETE
- Date/time: 2026-08-14 20:42:45 CST (Asia/Shanghai)
- Branch: `dev/oae-v0.1`

## 2. Scope

- Assigned objective: Materialize the approved OAE v0.1 raw/core persistent
  data contract as the first DuckDB DDL migration.
- Explicit in-scope work: Create `sql/001_core.sql`; create metadata-backed
  schema-contract tests that execute the SQL in temporary file-backed DuckDB
  databases; implement the six authorized tables and exact frozen keys,
  types, nullability, and checks; apply S1.03B Errata B1–B4; verify permissive
  raw NBBO evidence storage and the absence of research/audit scope.
- Explicit out-of-scope work: Application migration runner or loader;
  Pydantic DTOs; ingestion; research or audit schemas/tables; derived tables;
  foreign keys; additional indexes; defaults; table-level idempotency design;
  dependency, source-code, configuration, CI, canonical specification, or
  governance changes; and S1.09.
- Acceptance criteria: The migration executes successfully against a fresh
  database; creates only `meta`, `core`, and the six authorized base tables;
  implements every exact column/type/nullability/key/check contract including
  B1–B4; preserves invalid/crossed/zero-size/duplicate-like raw NBBO rows;
  passes focused, full, config, integrity, and exact-code CI verification; and
  is recorded through separate pushed implementation and report commits.

## 3. Specification Baseline

- Frozen Math Spec SHA256:
  `a612c66a23206e43b149c207f88a3de26e8c8e037cbf4eddd5ac9a3289e15dbe`
- Data/Backtest Spec SHA256:
  `81e0cd4f1b67cf29bb9eb1b2c6669ede750483027328cfb72d4c4c8b3252dd3f`
- Implementation Spec SHA256:
  `68a9a1e576d7f0d649e1c74885bd7327df565df7e9fc1c91667110b15aef7970`
- Implementation Errata SHA256:
  `67dda37487d03902af90fd03a6afd8e24c4f962e64cdcadfda971ec10cd4fc6f`
- CHANGE_CONTROL SHA256:
  `6f39a7f04ff9ca2fbd8aec0baa17ddeb883bd8e63866cbbc42c50b9b517de964`
- SPEC_MANIFEST SHA256:
  `46553a4d7e964856bb974c417dec36a62c697a5242348de2531abe78774de2a7`
- RAW CONFIG SHA256:
  `a8043d2e8959fa8073951406bdef103afb1a45163861db09cdd66a261f3f6fa0`
- NORMALIZED CONFIG SHA256:
  `ece897f1dd038743462322480aec20aba05b219ce7af8b2e4e38f7a013f11044`

All five manifest-listed canonical files, the manifest identity, raw YAML
identity, and normalized frozen configuration identity matched the approved
baseline before implementation and at the implementation-commit checkpoint.

## 4. Git Baseline

- Base commit SHA: `a781307752bfbed0e42099c5306e20433839bdb0`
- Branch: `dev/oae-v0.1`
- Initial working-tree status: Clean
- Expected remote: `https://github.com/XXHANOO/OAE.git`, branch
  `refs/heads/dev/oae-v0.1`

## 5. Files Changed

### Added

- `sql/001_core.sql`: Defines only the corrected `meta` and `core` schemas and
  six authorized raw/core base tables.
- `tests/unit/test_core_schema.py`: Executes the migration in pytest
  `tmp_path` databases and enforces CORE-SQL-001 through CORE-SQL-013 plus the
  absence of unauthorized foreign keys, unique constraints, defaults, and
  indexes.
- `docs/devlog/S1.08_core_sql.md`: Records the immutable S1.08 scope,
  implementation, and verification evidence.

### Modified

- `docs/devlog/INDEX.md`: Records the Human + ChatGPT approval of S1.07 and
  registers S1.08 as COMPLETE with review PENDING.

### Deleted

- NONE

`sql/.gitkeep` remains present and unchanged.

## 6. Implementation Details

`sql/001_core.sql` follows the authorized statement order: `meta` schema,
`core` schema, then `meta.raw_file_manifest`, `core.trading_sessions`,
`core.index_bars_1m`, `core.option_contracts`, `core.option_nbbo`, and
`core.option_settlements`. Only schema creation uses `IF NOT EXISTS`; table
creation does not introduce an idempotency policy.

The tests read `sql/001_core.sql` explicitly, use `oae.db.open_database(...)`,
execute the DDL against fresh temporary file-backed databases, interrogate
DuckDB `information_schema` metadata for ordered columns, canonical types,
nullability, table inventory, schema inventory, and ordered primary keys, and
close every connection explicitly in a fixture `finally` block.

The canonical `option_type IN ('C','P')` and
`settlement_style IN ('AM','PM')` checks are retained and tested by successful
canonical inserts and rejected invalid inserts. No exercise-style check,
foreign key, standalone unique constraint, default, or index was introduced.

## Core Persistent Data Contract

Schemas created:

```text
meta
core
```

Tables created:

```text
meta.raw_file_manifest
core.trading_sessions
core.index_bars_1m
core.option_contracts
core.option_nbbo
core.option_settlements
```

- Research schema created: NO
- Audit schema created: NO
- Research tables created: NO
- Audit tables created: NO
- Migration runner created: NO
- Ingestion code created: NO
- Pydantic DTOs created: NO

Frozen primary keys verified in column order:

```text
meta.raw_file_manifest: (raw_file_id)
core.trading_sessions: (session_date_et)
core.index_bars_1m: (symbol, bar_end_ts_utc)
core.option_contracts: (contract_id)
core.option_nbbo: NONE
core.option_settlements: (product, expiration_date, settlement_style)
```

## S1.03B Errata B Implementation

B1:

```text
IMPLEMENTED
core.index_bars_1m.quality_status VARCHAR NOT NULL
```

B2:

```text
IMPLEMENTED
core.option_contracts.expiration_ts_utc TIMESTAMPTZ NOT NULL
core.option_contracts.first_seen_date DATE NOT NULL
core.option_contracts.last_seen_date DATE NOT NULL
core.option_contracts.source VARCHAR NOT NULL
```

B3:

```text
IMPLEMENTED
core.option_nbbo.quote_condition VARCHAR NULLABLE
```

B4:

```text
IMPLEMENTED
core.option_settlements.raw_file_id VARCHAR NOT NULL
```

The old Implementation Specification section 28 was NOT copied verbatim. Its
known omissions were repaired exactly through canonical S1.03B Errata. No
broad Data-Spec-over-Implementation-Spec precedence rule was inferred.

Critical nullable boundaries were preserved and tested:

```text
core.option_contracts.series_type: NULLABLE
core.option_nbbo.quote_condition: NULLABLE
core.option_settlements.published_ts_utc: NULLABLE
```

## Raw Quote Storage Boundary

Raw `core.option_nbbo` persistence does NOT enforce quote-quality rules.

Crossed, locked, zero-size, otherwise invalid, and duplicate-like raw
observations remain storable for later quality classification. The contract
test inserts the same crossed quote with zero displayed sizes twice and
verifies both rows persist. No bid/ask validity, size validity, uniqueness, or
freshness rule is enforced by DDL.

## 7. Tests and Verification

Mandatory preflight:

```text
git branch --show-current
git rev-parse HEAD
git status --short
git remote -v
git ls-files
git log --graph --decorate --oneline --all --date-order --max-count=20
find sql tests/unit -maxdepth 2 -print | sort
shasum -a 256 -c docs/specs/SPEC_MANIFEST.sha256
shasum -a 256 docs/specs/SPEC_MANIFEST.sha256
shasum -a 256 config/oae_v0_1_frozen.yaml
wc -c config/oae_v0_1_frozen.sha256
sed -n 'l' config/oae_v0_1_frozen.sha256
```

Result: Branch, base HEAD, remote, and clean tree matched the assigned
baseline. All five manifest entries returned `OK`; manifest and raw-config
hashes matched; the normalized hash file was exactly 65 bytes and contained
the approved 64-character digest plus one LF. `sql/` contained only the
unchanged `.gitkeep`; no prior migration existed.

Local runtime and focused core-schema tests:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python --version
/private/tmp/oae-s107.q3CBSD/venv/bin/python -c 'import duckdb; print(duckdb.__version__)'
/private/tmp/oae-s107.q3CBSD/venv/bin/python -m pytest tests/unit/test_core_schema.py
```

Result:

- Python: `3.12.14`
- DuckDB: `1.5.5`
- Focused schema tests: 14 passed, 0 failed, 0 skipped in 0.25 seconds

Focused evidence included:

- successful DDL execution in fresh temporary file-backed databases;
- exactly `meta` and `core`, with no `research` or `audit` schema;
- exactly the six listed base tables and no derived/research/audit tables;
- exact ordered columns, DuckDB canonical types, and nullability for every
  table;
- all five ordered frozen primary keys and no PK on `core.option_nbbo`;
- B1 `quality_status`, all four B2 fields, nullable B3 `quote_condition`, and
  B4 `raw_file_id`;
- rejection of invalid option type and settlement style while canonical values
  insert successfully and exercise style remains unconstrained at DDL level;
- nullable `series_type`, `quote_condition`, and `published_ts_utc` inserts;
- two successful inserts of a crossed, zero-size, duplicate-like raw NBBO;
- absence of foreign keys, standalone unique constraints, defaults, and
  additional indexes.

Full local regression suite:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python -m pytest
```

Result: 112 passed, 0 failed, 0 skipped in 0.44 seconds. The approved S1.07
baseline of 98 tests remained green, and the 14 S1.08 tests passed.

Explicit frozen config regression check:

```text
/private/tmp/oae-s107.q3CBSD/venv/bin/python - <<'PY'
from oae.config import compute_config_sha256, load_config, validate_frozen_v0_1

config = load_config("config/oae_v0_1_frozen.yaml")
digest = compute_config_sha256(config)

assert digest == (
    "ece897f1dd038743462322480aec20aba05b219ce7af8b2e4e38f7a013f11044"
)

assert validate_frozen_v0_1(config) is None

print(digest)
PY
```

Result: The normalized digest was exactly
`ece897f1dd038743462322480aec20aba05b219ce7af8b2e4e38f7a013f11044`,
and validation returned `None`.

Pre-commit integrity and scope verification:

```text
shasum -a 256 -c docs/specs/SPEC_MANIFEST.sha256
shasum -a 256 docs/specs/SPEC_MANIFEST.sha256 config/oae_v0_1_frozen.yaml
wc -c config/oae_v0_1_frozen.sha256
sed -n 'l' config/oae_v0_1_frozen.sha256
git diff --check
git diff --name-status
git diff --stat
git status --short
git diff --exit-code HEAD -- sql/.gitkeep src pyproject.toml .github config AGENTS.md README.md docs tests/test_bootstrap.py tests/unit/test_config.py tests/unit/test_config_hash.py tests/unit/test_db.py tests/unit/test_enums.py data
find sql -maxdepth 1 -type f -print | sort
git diff --cached --check
git diff --cached --name-status
git diff --cached --stat
```

Result: All integrity and whitespace checks passed, every protected path and
`sql/.gitkeep` remained unchanged, no later SQL or lockfile was created, and
the staged implementation scope was exactly:

```text
A  sql/001_core.sql
A  tests/unit/test_core_schema.py
```

GitHub Actions verification for exact code commit
`bdaa1b42c471bbae2ce13da445dc25b35ac56c94`:

```text
gh run list --workflow CI --branch dev/oae-v0.1 --commit bdaa1b42c471bbae2ce13da445dc25b35ac56c94 --json databaseId,url,workflowName,status,conclusion,event,headSha --limit 5
gh run watch 31801418389 --exit-status
gh run view 31801418389 --json databaseId,url,workflowName,status,conclusion,event,headSha,jobs
gh run view 31801418389 --log
```

Result:

- Workflow: `CI`
- Run ID: `31801418389`
- Run URL: `https://github.com/XXHANOO/OAE/actions/runs/31801418389`
- Exact head SHA: `bdaa1b42c471bbae2ce13da445dc25b35ac56c94`
- Job: `test`; job ID `94770058716`
- Status: completed
- Conclusion: SUCCESS
- Checkout: PASS
- Five-entry manifest verification: PASS
- Python setup: PASS, CPython `3.12.13`
- Fresh editable install: PASS, including DuckDB `1.5.5`
- Package import: PASS
- Full pytest: 112 passed, 0 failed, 0 skipped in 1.03 seconds

## 8. Specification Compliance

- Frozen mathematics changed: NO
- Feature definitions changed: NO
- Market-data semantics changed: NO — the already-frozen raw/core contract was
  materialized without alteration
- Timestamp semantics changed: NO
- Execution assumptions changed: NO
- Risk/Edge thresholds or gate behavior changed: NO
- Frozen YAML or normalized identity changed: NO
- Canonical specifications, errata, manifest, or governance changed: NO
- Dependencies changed: NO
- CI workflow changed: NO
- Application source code changed: NO
- Corrected core SQL migration added: YES — exactly the assigned S1.08 scope
- S1.03B B1–B4 implemented: YES
- Research/audit schema or table created: NO
- Migration runner, DTO, or ingestion code created: NO
- Foreign key, additional index, default, or unauthorized constraint added: NO
- Assigned scope exceeded: NO
- Look-ahead exposure introduced: NO

## 9. Deviations

NONE

## 10. Known Limitations / Risks

- AGENTS report-template Errata SHA omission: UNRESOLVED P3; explicitly
  compensated for in this report.
- Warm-up 504–755 semantics: UNRESOLVED — future model-readiness stage.
- Branch protection: NOT ADDRESSED.
- GitHub Action SHA pinning: NOT ADDRESSED.
- Build backend full locking: NOT ADDRESSED.
- No research schema/table yet — S1.09.
- No audit schema/table yet — S1.10.
- No migration runner exists.
- No DTOs exist yet.
- No ingestion exists yet.
- No foreign-key lineage enforcement exists.
- No extra indexes exist.

These are intentional stage boundaries.

## 11. Verification State

- Working tree clean at code-commit checkpoint: YES
- Local HEAD at report creation:
  `bdaa1b42c471bbae2ce13da445dc25b35ac56c94`
- Remote HEAD at report creation:
  `bdaa1b42c471bbae2ce13da445dc25b35ac56c94`
- Code commit pushed: YES
- Exact code-commit CI completed successfully: YES
- Report commit pushed: PENDING AT REPORT CREATION; verify after commit without
  rewriting this historical report.
- Final report-commit CI: PENDING AT REPORT CREATION; verify after push without
  rewriting this historical report.

## 12. Next Proposed Stage

S1.09 — Research SQL Migration, only after Human + ChatGPT approve S1.08.

STOPPING FOR HUMAN / CHATGPT AUDIT.
